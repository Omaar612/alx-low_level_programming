#include "main.h"
/**
 * puts_half - print half of a string.
 *
 *@str: string.
 *
 * Return: Always void.
 */
void puts_half(char *str)
{
int i, x = 0;
for (i = 0; s[i] != '\0'; i++)
{
x++;
}
x = x / 2;
for (i = x; s[i] != '\0'; i++)
{
printf("%c", s[i]);
}
printf("\n");
}
