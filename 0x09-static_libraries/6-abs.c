#include "main.h"
/**
 * _abs - chack for the absulute.
 *
 *@c: input for alphabet
 *
 * Return: (c)success, Return: (-c)success.
 */
int _abs(int c)
{
if (c >= 0)
{
return (c);
}
else
{
return (-c);
}
}
